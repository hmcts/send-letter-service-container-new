function:
  # Don't modify below here
  image: ${IMAGE_NAME}
  triggerAuthName: "azure-blob-auth"
  triggers:
    - type: azure-blob
      blobContainerName: "new"
      accountName: "rpesendletterpreview"